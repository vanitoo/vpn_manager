# Telegram
BOT_TOKEN=123456:CHANGE_ME
ADMIN_IDS=123456789
DB_PATH=data/vpn_bot.sqlite3

# При первом запуске создаёт демо-тарифы на 30 и 90 дней.
SEED_PLANS_ON_START=true

# Polling
DELETE_WEBHOOK_ON_START=true
DROP_PENDING_UPDATES=false
AUTO_SETUP_BOT_MENU=true

# Optional proxy for Telegram Bot API
PROXY_MODE=off
PROXY=
PROXY_HEALTHCHECK_URL=https://api.telegram.org
PROXY_HEALTHCHECK_TIMEOUT=8
PROXY_HEALTHCHECK_INTERVAL=60

# Payments
# Possible: stars,yookassa
PAYMENT_PROVIDERS=stars
ENABLE_STARS=true
YOOKASSA_ENABLED=false

# Stars conversion: stars = ceil(price_rub / STARS_RUB_PER_STAR)
STARS_RUB_PER_STAR=1.70

# YooKassa. The bot works without webhook: it opens payment URL and checks status via API.
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=
YOOKASSA_RETURN_URL=
YOOKASSA_TEST_MODE=true

# Receipt contact settings for YooKassa.
# For initial tests false is simpler, a fallback email is sent to YooKassa.
RECEIPT_REQUIRE_CONTACT=false
RECEIPT_FALLBACK_EMAIL=orders@example.com
RECEIPT_SAVE_CONTACT=true
PENDING_PAYMENT_TTL_MINUTES=60

# Remnawave
# Fill both values to enable real API requests.
# Until then the bot works in safe stub mode and creates example subscription URLs.
REMNAWAVE_BASE_URL=
REMNAWAVE_API_TOKEN=
# Fallback or public subscription prefix, for example https://sub.example.com
REMNAWAVE_SUBSCRIPTION_BASE_URL=
# 0 = use tariff traffic_gb (0 also means unlimited in the current MVP)
REMNAWAVE_DEFAULT_TRAFFIC_GB=0

# Logs
LOG_LEVEL=INFO
LOG_FILE=logs/bot.log
LOG_MAX_BYTES=10485760
LOG_BACKUP_COUNT=5
ADMIN_NOTIFY_PURCHASES=true
